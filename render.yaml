services:
  - type: web
    name: Time-are2
    env: node
    repo: https://github.com/Mohamed-Hackrs/TokenServer-nodejs.git
    rootDir: .
    buildCommand: npm install
    startCommand: npm start
    plan: free
    envVars:
      - key: APP_ID
        sync: false
      - key: APP_CERTIFICATE
        sync: false
